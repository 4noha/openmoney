# OpenMoney — Claude Instructions

## このプロジェクトで遊ぼう

OpenMoney はオープンソースです。**自分の環境に合ったスクレイパーを追加したり、家計分析の新機能を作ったり、バグを直してプルリクエストを送ったり**、自由に改造してください。

### Claude Code で開発する（推奨）

このリポジトリは **Claude Code** を使って開発されています。ターミナルで `claude` を起動するだけで、この CLAUDE.md を読んだ状態でコーディングを手伝ってもらえます。

```bash
# リポジトリのルートで
claude
```

**Claude Code は月 $20 の Pro プランで使えます。** コーディング支援だけでなく、調査・文書作成・データ分析・他のプロジェクトへの活用など、あらゆる知的作業に使えるため、開発者にとって費用対効果が非常に高いツールです。

> Claude Code について詳しくは https://claude.ai/code

### アプリ内 AI のコストを抑える

OpenMoney はレシート OCR に Anthropic API（従量課金）を使っています。**開発作業そのものに Anthropic API を呼ばないよう**、開発は Claude Code 経由で行い、アプリ側の AI 利用はレシート読み取りだけに限定することを推奨します。

```toml
# config/user.toml — AI 機能はレシート OCR のみ有効にする
# (他の AI 機能を追加実装する場合もここで on/off できるようにしてください)
```

これにより API 費用はレシート 1 枚あたり数円程度に抑えられます。

### 「手作業ゼロ」がコンセプト — Claude Code が代わりにやる

OpenMoney は**明細の手動追加・削除・編集 UI を意図的に持ちません**。データはスクレイパーが自動で取得し、DB は常にスクレイプ結果が正とみなされます。

その代わり、「やりたいこと」は Claude Code に頼めばコードに落とせます:

**破壊的な操作（データ修正・削除）**
```
「Amazon の ○○ という注文を transactions から削除して」
「2024年以前の Mercari 売上をすべて消して」
```
→ Claude Code が適切な SQL や Python スクリプトを書いて実行します。UI を作るより安全で、操作の意図もコミット履歴に残ります。

**仕訳ルールのビルド（経費・個人支出の自動分類）**
```
「AWS の請求はすべて経費・通信費に仕訳して」
「Makuake の支援は個人支出として扱って」
「家賃引落しは按分率 80% で経費にして」
```
→ `config/aoiro_rules.toml` や `src/aoiro/` のルールエンジンに設定を追加します。一度ルールを書けば、以後のスクレイプで自動的に仕訳が生成されます。

このワークフローにより「UI を触る手間」ではなく「ルールを育てる投資」になります。

### 自動化したいことは何でも Claude Code に言えばいい

このプロジェクトは「あなたの家計」のためのツールです。市販アプリと違い、**あなたが欲しいものをそのまま作れます**。

やりたいことを日本語で伝えるだけで、Claude Code がオーダーメイドのシステムを組み上げます:

**集計・レポート**
```
「毎月のカテゴリ別支出を Slack に通知して」
「年間の光熱費推移をグラフで見たい」
「サブスク費用だけを一覧で出して月の合計を教えて」
```

**アラート・監視**
```
「同じ店で月3回以上使ったら警告して」
「先月より支出が 20% 増えたカテゴリを教えて」
「定期引落が来なかったら通知して」
```

**連携・エクスポート**
```
「確定申告用に経費を CSV で出力して」
「freee / マネーフォワード クラウド に取り込める形式でエクスポートして」
「Google スプレッドシートに毎週自動で書き出して」
```

**スクレイパーの追加**
```
「楽天証券の残高も取り込みたい」
「電気代の明細を自動で取得したい」
```

いずれも「言えば作れる」ものばかりです。市販のアプリで「この機能があれば…」と思ったことが、そのまま実現できます。コードはすべてあなたのローカルに残り、サービス終了の心配もありません。

### すべて手元に残る — パスワードも、データも

OpenMoney はクラウドにデータを送りません。

- **パスワードは暗号化してローカルに保存** — 各金融機関のログイン情報は AES-256 で暗号化され、あなたの PC の `.env` と DB だけに存在します。外部サービスに預けることはありません
- **明細データはローカル SQLite** — スクレイプした入出金・残高・注文履歴はすべて `transactions.db` に蓄積されます。クラウド同期なし、API 送信なし
- **レシート OCR だけ外部 API を使用** — 写真からレシートを読み取るときのみ Anthropic API を呼びます。それ以外の通常操作では外部通信は発生しません
- **サービス終了がない** — コードとデータが手元にある限り、プロバイダの都合で突然使えなくなることはありません

銀行のパスワードを他社サーバーに預けることに不安を感じていたなら、OpenMoney はその問題を根本から解決します。

### 外出先からアクセスする — Tailscale のセットアップ

OpenMoney は自宅 PC で動くローカルサーバーです。外出先のスマートフォンやノート PC からもアクセスするには **Tailscale** が最も簡単です。

**Tailscale とは**: デバイス間に暗号化された仮想プライベートネットワーク (VPN) を作るツール。自宅 PC とスマホを同じ「プライベートネットワーク」に繋ぎ、外出先から `http://your-pc.tailnet.ts.net:8765/` で OpenMoney に直接アクセスできます。ポート開放やドメイン取得は不要です。

**セットアップ手順**:
1. [tailscale.com](https://tailscale.com) からインストール (無料プランで十分)
2. 自宅 PC と外出先デバイス (スマホ・ノート PC) の両方でログイン
3. OpenMoney の設定画面を開くと Tailscale URL が自動で表示されます

OpenMoney は起動時に `tailscale status` を自動検知し、MagicDNS ホスト名または Tailscale IP から外部アクセス URL を組み立てます。設定画面 (`/settings`) で現在の URL を確認できます。

> 外出中に「あの支出いくらだっけ？」と残高や明細を確認したりするのに便利です。
>
> **レシート登録は Tailscale なしでも可能** — Android アプリ (OpenMoney Eclipse) からレシートを撮影すると、Firebase Storage → Firestore 経由で自宅 PC に中継されます。外出先と自宅 PC が直接つながっていなくても、撮った瞬間に OCR キューに積まれ、PC がオンラインになったタイミングで自動処理されます。

### コントリビューション歓迎

- **新しいスクレイパー**: `plugins/` 以下に新しいディレクトリを作るだけ。`plugin.py` に `PluginSpec` を定義すれば自動でデーモンに組み込まれます
- **UI 改善**: `src/server/templates/` の HTML/JS を直接編集。FastAPI + Jinja2 なのでフレームワーク不要
- **青色申告エンジン**: `src/aoiro/` に仕訳ルールや集計ロジックの改善を
- **バグ報告・PR**: GitHub の Issue / Pull Request でお気軽に

---

## プロジェクト概要

銀行・証券・クレジットカードなどの金融機関にスクレイピングでログインし、入出金・残高データをローカル DB に蓄積するツール。

## 関連プロジェクト

- `OpenMoney Eclipse`（Android）— プッシュ通知を読み取り、二段階認証コードをこちらに返す Android アプリ

## 技術方針（暫定）

- スクレイピング: Playwright（Python）を想定
- DB: SQLite（ローカル運用のためシンプルに）
- 二段階認証連携: Android アプリとのローカル通信（ADB または HTTP）

## 注意事項

- 認証情報（パスワード・APIキー等）は `.env` に記載し、絶対にコミットしない
- スクレイピング対象の利用規約・レート制限を尊重する
- `OpenMoney Eclipse` との通信インターフェース変更時は両リポジトリを同時に更新する

## 実装方針

### データ整合性は「対処療法」ではなく「恒久的なスクリプト改善」で守る

このプロジェクトの DB はスクレイパーが定期実行で繰り返し書き換える。一度 SQL や手作業で表面的に直しても、次のスクレイプで同じ不整合が再発したら無意味。バグ修正は **スクレイパー / パーサ / split / 自動分類 / タグ判定** の該当層に恒久的な改善として入れること。

具体ガイド:

- **Root-cause を直す**: 「DB を直接 UPDATE して見た目を治す」「特定 order_id だけ手動で再フェッチ」のような対処療法は、**コード側に同じ修正を入れた後**の補助手段にとどめる。コードに反映しないまま DB だけ直すと、次回スクレイプで上書きされる／再発する。
- **自己修復ロジックを優先**: 旧パーサで保存された不整合を **次回スクレイプで自動検知 → リセット → 再取得** する仕組みを入れる（例: `fetch_order_details` 冒頭の `items_sum != item_subtotal` 不整合チェック、`order_total=0 + points_used>0` の debit 自動書き戻し）。手動 sweep が要らなくなる。
- **idempotent に書く**: `split_orders` `_upsert_split_row` のように、何度実行しても同じ結果になるよう実装する。途中で止まっても再実行で復旧できる状態を保つ。
- **過剰防御を入れる**: パーサの価格 floor / 累計到達ガード / 累計超過ガード / 推薦マーカー除外など、複数のチェックを多層で入れる。1 つすり抜けても次の層で止まるようにする。
- **「症状」ではなく「条件」でコードを直す**: 「ナニワ金融道で ¥1 が落ちた」を直すなら `price >= 10` を `price >= 1` に下げる（コード）。特定 order だけ再フェッチして帰結を治すのではない。
- **タグ・カテゴリは表示時に動的に計算**: スクレイパーが書く DB データは生のまま、`_fetch_base_rows` 等の API 層で `amazon_points_full` / `paypay` / `返品` 等を毎回計算する。判定基準が変わってもデータマイグレーション不要。
- **コミットメッセージで因果を残す**: 「なぜこの行を変えたか」「どんな入力で再現する不具合か」を本文に書く。後から見返したとき「あれ、この防御要らないんじゃ」と消されるのを防ぐ。

README の「Amazon 注文の取り込みパイプライン」セクションに現状の防御層が一覧化されている（パーサの多段ガード、自己修復、ポイント全額決済の自動補正、返品の category 上書き、シリーズ折り畳み等）。新しい不整合を見つけたら、まず README の該当層に追加修正できるか／新しい層が必要かで考える。
